<Window x:Class="FtpVirtualDrive.UI.Views.FtpFileExplorerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FTP File Explorer" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- File icon converter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        
        <!-- Styles -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="2" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="Background" Value="#f0f0f0" />
            <Setter Property="Border.CornerRadius" Value="3" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e0e0e0" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#d0d0d0" />
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="FileListItemStyle" TargetType="ListViewItem">
            <Setter Property="Padding" Value="4" />
            <Setter Property="Margin" Value="1" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f0f8ff" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0078d4" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Background="#f8f9fa" Padding="5">
            <Button Content="🔄 Refresh" 
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource ToolbarButtonStyle}"
                    ToolTip="Refresh current directory" />
            
            <Button Content="⬆️ Up" 
                    Command="{Binding NavigateUpCommand}"
                    Style="{StaticResource ToolbarButtonStyle}"
                    ToolTip="Go to parent directory" />
            
            <Separator />
            
            <Button Content="🗑️ Cleanup" 
                    Command="{Binding CleanupTempFilesCommand}"
                    Style="{StaticResource ToolbarButtonStyle}"
                    ToolTip="Clean up temporary files" />
            
            <Separator />
            
            <TextBlock Text="Temp Folder:" VerticalAlignment="Center" Margin="5,0" />
            <TextBox Text="{Binding TempFolderPath, Mode=OneWay}" 
                     IsReadOnly="True" 
                     MinWidth="200" 
                     VerticalAlignment="Center"
                     Background="White"
                     ToolTip="Temporary files location" />
        </ToolBar>
        
        <!-- Address Bar -->
        <Grid Grid.Row="1" Background="#ffffff" Margin="10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="📁 Current Path:" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0" 
                       FontWeight="SemiBold" />
            
            <TextBox Grid.Column="1" 
                     Text="{Binding CurrentPath, Mode=OneWay}" 
                     IsReadOnly="True"
                     Background="#f8f9fa"
                     Padding="8,4"
                     FontFamily="Consolas"
                     VerticalAlignment="Center" />
        </Grid>
        
        <!-- File List -->
        <ListView Grid.Row="2" 
                  ItemsSource="{Binding Files}"
                  SelectedItem="{Binding SelectedItem}"
                  ItemContainerStyle="{StaticResource FileListItemStyle}"
                  Background="White"
                  BorderBrush="#e1e5e9"
                  BorderThickness="1"
                  Margin="10,5,10,5">
            
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="300">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding IsDirectory, Converter={StaticResource BooleanToVisibilityConverter}}" 
                                               Visibility="Collapsed" />
                                    
                                    <!-- Directory Icon -->
                                    <TextBlock Text="📁" 
                                               Margin="0,0,5,0" 
                                               FontSize="16"
                                               Visibility="{Binding IsDirectory, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    
                                    <!-- File Icon -->
                                    <TextBlock Text="📄" 
                                               Margin="0,0,5,0" 
                                               FontSize="16">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    
                                    <TextBlock Text="{Binding Name}" 
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Size" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Size, StringFormat='{}{0:N0} bytes'}" 
                                           HorizontalAlignment="Right">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                    <Setter Property="Text" Value="&lt;DIR&gt;" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Modified" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Actions" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <!-- Open File Button -->
                                    <Button Content="📂 Open" 
                                            Command="{Binding DataContext.OpenFileCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Margin="2"
                                            Padding="4,2"
                                            FontSize="10"
                                            ToolTip="Download and open file">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    
                                    <!-- Navigate to Folder Button -->
                                    <Button Content="📁 Enter" 
                                            Command="{Binding DataContext.NavigateToFolderCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Margin="2"
                                            Padding="4,2"
                                            FontSize="10"
                                            ToolTip="Navigate to folder"
                                            Visibility="{Binding IsDirectory, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
            
            <!-- Double-click handlers -->
            <ListView.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" 
                              Command="{Binding OpenFileCommand}" 
                              CommandParameter="{Binding SelectedItem}" />
            </ListView.InputBindings>
        </ListView>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Background="#f8f9fa" Padding="10,5">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <!-- Loading Indicator -->
                    <TextBlock Text="⏳" 
                               Margin="0,0,5,0"
                               Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    
                    <TextBlock Text="{Binding StatusMessage}" 
                               VerticalAlignment="Center" />
                </StackPanel>
            </StatusBarItem>
            
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding Files.Count, StringFormat='Items: {0}'}" 
                           VerticalAlignment="Center" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>